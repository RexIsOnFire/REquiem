# ReQuiem has ZERO hard runtime dependencies — the core pipeline runs on the
# Python standard library alone. Everything below is OPTIONAL and enhances a
# specific stage; the code degrades gracefully when a package is absent.

# --- static analysis fidelity ---
pefile>=2023.2.7        # accurate PE import/export/rich-header parsing
lief>=0.14.0            # optional cross-format (PE/ELF/Mach-O) parsing
yara-python>=4.3.0      # YARA rule scanning
capstone>=5.0.0         # CFG disassembly of the entry point

# --- HTTP API ---
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.31 # required by FastAPI for file uploads

# --- Auth (multi-user accounts + encrypted per-user API keys) ---
PyJWT>=2.13.0            # session tokens
cryptography>=42.0.0   # Fernet encryption of stored keys
# (password hashing uses stdlib hashlib.scrypt; SQLite is stdlib)

# --- PDF report export (pick ONE; both optional) ---
# playwright is the most reliable cross-platform choice (esp. Windows):
#     pip install playwright && playwright install chromium
# weasyprint is pure-Python but needs native GTK/Pango libs on Windows.
playwright>=1.44.0
# weasyprint>=62.0

# --- dev / test ---
pytest>=8.0.0
